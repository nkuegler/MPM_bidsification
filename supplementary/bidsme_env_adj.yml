name: bidsme_env_adj
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.12
  - ipykernel
  - jupyterlab
  - scipy
  - matplotlib
  - pandas > 1.4
  - ruamel.yaml >= 0.15.35
  - coloredlogs
  - pyparsing
  - bidsschematools
  - pydicom >= 1.4.2
  - nibabel
  - dcm2niix >= 1.0.20220715
  - pip
  - pip:
    - -e /data/u_kuegler_software/git/bidsme_adj/
